<header>
  <h2 id="title">Safemeet</h2>
</header>

{{outlet "menu"}}

{{outlet "map"}}

{{outlet "locations"}}

{{outlet}}
<footer>
  {{#if session.isAuthenticated}}
    <a {{action "invalidateSession"}} href="javascript:void(0)">Logout</a>
  {{else}}
    {{link-to "Login" 'user.login'}}
  {{/if}}
  Made 2015 by Alex
</footer>
